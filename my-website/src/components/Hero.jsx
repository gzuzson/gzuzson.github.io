const Hero = () => {
    const styles = {
        header: {
            backgroundColor: "yellow",
        },
    };

    return (
      <h1 style={styles.header}>Hello, World</h1>
    );
}

export default Hero;